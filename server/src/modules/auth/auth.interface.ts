/**
 * Auth Module — Interface.
 *
 * Contracts that the AuthService and AuthRepository must implement.
 * Controllers and middleware depend on these interfaces, not concrete classes.
 */

export type { IAuthService } from '../../interfaces/service.interface';
